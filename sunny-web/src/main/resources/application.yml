server:
  servlet:
    context-path: /sunny-web
    port: 8080

spring:
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    check-template-location: true
    suffix: .html
    encoding: UTF-8
    servlet:
      content-type: text/html
    mode: HTML

##数据库连接配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sunny?characterEncoding=UTF-8
    username: root
    password: s@123456
    type: com.alibaba.druid.pool.DruidDataSource

##mybatis配置
#mybatis:
#  mapper-locations: classpath:mybatis/mapper/*.xml

